% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_graph_series.R
\name{build_graph_series}
\alias{build_graph_series}
\title{Build a time series of graph snapshots}
\usage{
build_graph_series(
  data,
  window = c("day", "week", "month", "quarter", "year"),
  directed = TRUE,
  weighted = TRUE,
  remove_self_loops = TRUE
)
}
\arguments{
\item{data}{A standardized social event tibble (output of \code{\link[=as_social_events]{as_social_events()}}).}

\item{window}{Aggregation window: one of \code{"day"}, \code{"week"}, \code{"month"},
\code{"quarter"}, or \code{"year"}. Default \code{"month"}.}

\item{directed}{Logical; if \code{TRUE} (default) graphs are directed.}

\item{weighted}{Logical; if \code{TRUE} (default) edges carry aggregated weights.}

\item{remove_self_loops}{Logical; if \code{TRUE} (default) self-loops are removed.}
}
\value{
A named list of igraph objects of class \code{social_graph_series}.
Names are ISO date strings representing the start of each period.
}
\description{
Splits event data into non-overlapping time windows and builds one graph
per window. Returns a named list of igraph objects.
}
\examples{
data(sim_social_events)
ev <- as_social_events(sim_social_events)
gs <- build_graph_series(ev, window = "month")
length(gs)
names(gs)

}
