% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engagement_gap.R
\name{audit_group_disparities}
\alias{audit_group_disparities}
\title{Audit network metrics by user group over time}
\usage{
audit_group_disparities(
  data,
  graph_series,
  group_var = "actor_group",
  window = c("month", "week", "day", "quarter", "year")
)
}
\arguments{
\item{data}{A standardized social event tibble with group membership columns.}

\item{graph_series}{A \code{social_graph_series}.}

\item{group_var}{Column name identifying actor groups. Default \code{"actor_group"}.}

\item{window}{Aggregation window. Default \code{"month"}.}
}
\value{
A tibble with one row per period x group, including:
\code{mean_indegree}, \code{mean_outdegree}, \code{mean_betweenness}, \code{isolation_rate}
(proportion of isolated nodes), and \code{n_users}.
}
\description{
Computes per-group summaries of structural position (degree, betweenness)
across all periods in a graph series. Useful for detecting systematic
disparities in network access or visibility.
}
\examples{
data(sim_social_events)
ev <- as_social_events(
  sim_social_events,
  actor_group  = "actor_group",
  target_group = "target_group"
)
gs <- build_graph_series(ev, window = "month")
audit_group_disparities(ev, gs)

}
