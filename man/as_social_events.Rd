% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_social_events.R
\name{as_social_events}
\alias{as_social_events}
\title{Coerce a data frame into a standardized social event table}
\usage{
as_social_events(
  data,
  actor = "actor_id",
  target = "target_id",
  time = "timestamp",
  event_type = NULL,
  weight = NULL,
  actor_group = NULL,
  target_group = NULL
)
}
\arguments{
\item{data}{A data frame or tibble containing interaction events.}

\item{actor}{Column name (character) for the source actor. Default \code{"actor_id"}.}

\item{target}{Column name (character) for the target actor. Default \code{"target_id"}.}

\item{time}{Column name (character) for the event timestamp. Must be
\code{POSIXct}, \code{POSIXt}, or \code{Date}. Default \code{"timestamp"}.}

\item{event_type}{Optional column name (character) for interaction type
(e.g., \code{"follow"}, \code{"reply"}). If \code{NULL}, defaults to \code{"interaction"}.}

\item{weight}{Optional column name (character) for edge weight. If \code{NULL},
defaults to \code{1} for all events.}

\item{actor_group}{Optional column name (character) for actor group membership
(used in group disparity analyses).}

\item{target_group}{Optional column name (character) for target group membership.}
}
\value{
A tibble of class \code{social_events} with standardized columns:
\code{actor_id}, \code{target_id}, \code{timestamp}, \code{event_type}, \code{weight}, and
optionally \code{actor_group}, \code{target_group}.
}
\description{
Validates and standardizes a data frame of user-to-user interaction events
for use in temporal social network analysis with \pkg{socialdrift}.
}
\examples{
events <- data.frame(
  from   = c("u1", "u1", "u2", "u3"),
  to     = c("u2", "u3", "u3", "u4"),
  when   = as.POSIXct(c("2025-01-01", "2025-01-03",
                         "2025-01-04", "2025-01-06"))
)
ev <- as_social_events(events, actor = "from", target = "to", time = "when")
ev

}
