% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_network_metrics.R
\name{plot_network_metrics}
\alias{plot_network_metrics}
\title{Plot one or more time-varying network metrics}
\usage{
plot_network_metrics(
  data,
  metric = NULL,
  title = "Network Metrics Over Time",
  colour = "#2c7bb6"
)
}
\arguments{
\item{data}{A tibble with a \code{period} column and at least one numeric metric.}

\item{metric}{Character vector of column name(s) to plot. If more than one,
a faceted chart is produced. If \code{NULL}, all numeric columns except
\code{period} are plotted.}

\item{title}{Plot title. Default \code{"Network Metrics Over Time"}.}

\item{colour}{Line colour. Default \code{"#2c7bb6"}.}
}
\value{
A \code{ggplot} object.
}
\description{
Produces a line chart of a metric (or faceted chart for multiple metrics)
from the output of metric functions such as \code{\link[=network_density_ts]{network_density_ts()}},
\code{\link[=reciprocity_ts]{reciprocity_ts()}}, or \code{\link[=summarize_network_series]{summarize_network_series()}}.
}
\examples{
data(sim_social_events)
ev  <- as_social_events(sim_social_events)
gs  <- build_graph_series(ev, window = "month")
tbl <- network_density_ts(gs)
plot_network_metrics(tbl, metric = "density")

}
