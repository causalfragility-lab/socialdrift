% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_graph_snapshot.R
\name{build_graph_snapshot}
\alias{build_graph_snapshot}
\title{Build a weighted graph snapshot from event data}
\usage{
build_graph_snapshot(
  data,
  start = NULL,
  end = NULL,
  directed = TRUE,
  weighted = TRUE,
  remove_self_loops = TRUE
)
}
\arguments{
\item{data}{A standardized social event tibble (output of \code{\link[=as_social_events]{as_social_events()}}).}

\item{start}{Optional start date/time (inclusive). Events before this are excluded.}

\item{end}{Optional end date/time (exclusive). Events from this point are excluded.}

\item{directed}{Logical; if \code{TRUE} (default) the graph is directed.}

\item{weighted}{Logical; if \code{TRUE} (default) edge weights reflect total
event count between each pair.}

\item{remove_self_loops}{Logical; if \code{TRUE} (default) self-loops are removed.}
}
\value{
An igraph object. Returns an empty graph if no events fall in the
specified window.
}
\description{
Aggregates event-level interactions into a directed or undirected igraph
object for a specified time window.
}
\examples{
data(sim_social_events)
ev <- as_social_events(sim_social_events)
g  <- build_graph_snapshot(ev)
igraph::vcount(g)
igraph::ecount(g)

}
