% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics_inequality.R
\name{degree_inequality_ts}
\alias{degree_inequality_ts}
\title{Compute degree inequality over time}
\usage{
degree_inequality_ts(graph_series, mode = c("all", "in", "out"))
}
\arguments{
\item{graph_series}{A \code{social_graph_series} (output of \code{\link[=build_graph_series]{build_graph_series()}}).}

\item{mode}{Degree mode: \code{"all"} (default), \code{"in"}, or \code{"out"}.}
}
\value{
A tibble with columns \code{period}, \code{degree_gini}, and \code{degree_mean}.
}
\description{
Uses the Gini coefficient of node degree as a measure of how unequally
connections are distributed across the network. A Gini of 0 means perfect
equality; 1 means one node holds all connections.
}
\examples{
data(sim_social_events)
ev <- as_social_events(sim_social_events)
gs <- build_graph_series(ev, window = "month")
degree_inequality_ts(gs)

}
