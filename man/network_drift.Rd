% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drift_network.R
\name{network_drift}
\alias{network_drift}
\title{Compute the Network Drift Index (NDI) over time}
\usage{
network_drift(graph_series, w1 = 0.3, w2 = 0.25, w3 = 0.25, w4 = 0.2)
}
\arguments{
\item{graph_series}{A \code{social_graph_series} (output of \code{\link[=build_graph_series]{build_graph_series()}}).}

\item{w1}{Weight for edge turnover. Default \code{0.30}.}

\item{w2}{Weight for degree distribution shift. Default \code{0.25}.}

\item{w3}{Weight for community structure change. Default \code{0.25}.}

\item{w4}{Weight for centralization change. Default \code{0.20}.}
}
\value{
A tibble with columns \code{period}, \code{edge_turnover},
\code{degree_shift}, \code{modularity_change}, \code{centralization_change}, and \code{ndi}.
}
\description{
The Network Drift Index quantifies how much a network's structure changes
between consecutive time periods. It combines changes in edge turnover,
degree distribution, community structure, and centralization into a single
composite score.
}
\details{
\deqn{NDI_t = w_1 \Delta_{edges} + w_2 \Delta_{degree} + w_3 \Delta_{community} + w_4 \Delta_{centrality}}

Default weights: \verb{w1 = 0.30, w2 = 0.25, w3 = 0.25, w4 = 0.20}.

Component definitions:
\describe{
\item{edge_turnover}{Jaccard distance between edge sets of adjacent snapshots.}
\item{degree_shift}{Jensen-Shannon-like divergence of degree distributions.}
\item{modularity_change}{Absolute change in community modularity.}
\item{centralization_change}{Absolute change in degree centralization.}
}
}
\examples{
data(sim_social_events)
ev <- as_social_events(sim_social_events)
gs <- build_graph_series(ev, window = "month")
network_drift(gs)

}
